# JWT Secret for signing tokens (make sure it's long and random)
JWT_SECRET=mySuperSecretJWTKey

# The port the app will run on (default to 5000 if not set)
PORT=5000

# (Optional) Enable debugging in development environment
DEBUG=true
